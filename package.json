{
  "devDependencies": {
    "@herb-tools/formatter": "^0.8.4",
    "@herb-tools/linter": "^0.8.4",
    "@herb-tools/rewriter": "^0.8.4",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "lint": "prettier --check app/**/*.js",
    "lint:fix": "prettier --write app/**/*.js",
    "herb:lint": "herb-lint",
    "herb:format": "herb-format",
    "herb:format:check": "herb-format --check"
  }
}
